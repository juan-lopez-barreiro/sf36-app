"use client";
import React from "react";
import SF36App from "./SF36App"; // ← mantén esta línea si ya tienes SF36App.tsx creado

export default function Page() {
  // Si SF36App aún no existe o no se encuentra, comenta la línea de arriba
  // y pega aquí directamente TODO el código de tu componente App del canvas,
  // con "use client" al principio del archivo.
  return <SF36App />;
}
